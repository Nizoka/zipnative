{
  "$comment": "Executable documentation. Each entry declares the recipe file, the task it demonstrates, the API surface it exercises, the version it first worked in, and machine-checkable 'expects' strings asserted by tests/docs/recipes.test.ts.",
  "recipes": [
    {
      "file": "read-list-entries.ts",
      "task": "Open an archive and list entries without decompressing anything",
      "surface": ["openZip", "entries"],
      "since": "0.1.0",
      "expects": ["entryCount=3", "names=dir/,hello.txt,data.bin"]
    },
    {
      "file": "random-access-single-entry.ts",
      "task": "Read exactly one entry from an archive, CRC-verified",
      "surface": ["openZip", "getEntry", "readEntry"],
      "since": "0.1.0",
      "expects": ["content=hello zipnative", "verified=true"]
    },
    {
      "file": "extract-safe.ts",
      "task": "Extract an untrusted archive in memory with security defaults",
      "surface": ["extractZip"],
      "since": "0.1.0",
      "expects": ["extracted=2", "traversal-rejected=true"]
    },
    {
      "file": "deterministic-build.ts",
      "task": "Build a reproducible archive — identical inputs, identical SHA-256",
      "surface": ["createZip", "toBytes"],
      "since": "0.2.0",
      "expects": ["identical=true"]
    },
    {
      "file": "create-streaming.ts",
      "task": "Create an archive with bounded memory via stream() and addStream()",
      "surface": ["createZip", "addStream", "stream"],
      "since": "0.2.0",
      "expects": ["multiple-chunks=true", "round-trip=true"]
    },
    {
      "file": "update-entry-in-place.ts",
      "task": "Replace one entry of an existing archive without recompressing the rest",
      "surface": ["createZipModifier", "replaceEntry", "save"],
      "since": "0.4.0",
      "expects": ["prefix-intact=true", "config={\"version\":2}"]
    },
    {
      "file": "iterate-stream.ts",
      "task": "Read a ZIP off an unseekable stream — forward CD-less iteration",
      "surface": ["iterateZipEntries"],
      "since": "0.5.0",
      "expects": ["entries=data/log.txt,manifest.json", "manifest={\"name\":\"streamed\"}"]
    },
    {
      "file": "parallel-create.ts",
      "task": "Create an archive across worker threads, byte-identical to the sequential writer",
      "surface": ["createParallelZip"],
      "since": "0.5.0",
      "expects": ["identical=true", "entries=3"]
    },
    {
      "file": "resumable-inflate.ts",
      "task": "Inflate a raw deflate stream chunk by chunk with exact consumed-byte reporting",
      "surface": ["createInflator"],
      "since": "0.8.0",
      "expects": ["consumed-exact=true"]
    },
    {
      "file": "custom-codec.ts",
      "task": "Register a custom compression codec and read a foreign-method archive",
      "surface": ["registerCodec", "crc32"],
      "since": "0.8.0",
      "expects": ["before-code=ZIP_UNSUPPORTED_METHOD", "after=exotic method payload"]
    },
    {
      "file": "sanitize-external-sink.ts",
      "task": "Apply zipnative's traversal gate to entry names in an external filesystem sink",
      "surface": ["sanitizeEntryPath"],
      "since": "0.8.1",
      "expects": ["traversal=REFUSED", "device=REFUSED", "clean=docs/readme.md"]
    },
    {
      "file": "verify-archive.ts",
      "task": "Deep-verify an archive in one non-throwing, machine-readable call",
      "surface": ["verifyZip"],
      "since": "0.9.0",
      "expects": ["good-ok=true", "bad-ok=false", "not-zip-code=ZIP_EOCD_NOT_FOUND"]
    }
  ]
}
